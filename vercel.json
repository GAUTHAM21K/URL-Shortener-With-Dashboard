{
  "version": 2,
  "buildCommand": "pip install -r backend/requirements.txt && cd frontend && npm install && npm run build && cd .. && mkdir -p backend/staticfiles && cp -r frontend/dist/* backend/staticfiles/ && cd backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && cd ..",
  "builds": [
    {
      "src": "backend/core/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.11"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/core/wsgi.py" }
  ],
  "env": {
    "DEBUG": "False",
    "PYTHONUNBUFFERED": "1"
  }
}